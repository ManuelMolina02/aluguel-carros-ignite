// descrição de um conjunto de testes

describe('Criar categoria', () => {
  // aplicando um teste - it = isto

  it('Espero que 2 + 2 seja 4', () => {
    const sum = 2 + 2;

    const result = 4;

    // espere que soma seja igual a resultado

    expect(sum).toBe(result);
  });
});
